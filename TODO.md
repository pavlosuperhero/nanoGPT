# TODO: nanoGPT MVP 2.0 Implementation Plan

## Фаза 1: Конвеєр даних (Memory Mapping & BPE)
- [ ] **1.1 Оновлення Токенізатора:** Замінити посимвольний `Tokenizer` на `tiktoken` (кодування `gpt2`).
  - *Дебаг:* Закодувати/декодувати тестовий рядок, перевірити ідентичність.
- [ ] **1.2 Memory Mapping:** Використати `numpy.memmap` для роботи з великими файлами прямо з диска.
  - *Дебаг:* Перевірити споживання RAM при відкритті файлу > 5GB (має бути < 1GB).
- [ ] **1.3 BatchGenerator 2.0:** Переписати зчитування батчів для прямої індексації `memmap` масиву.
  - *Дебаг:* Перевірити `xb.shape` та переконатися у відсутності `IndexError`.

## Фаза 2: Обчислювальне ядро (Compute Acceleration)
- [ ] **2.1 Рефакторинг Attention:** Злити клас `Head` у `MultiHeadAttention` для паралельних обчислень.
  - *Дебаг:* Звірити кількість параметрів моделі.
- [ ] **2.2 FlashAttention:** Замінити мануальний Softmax/Masking на `torch.nn.functional.scaled_dot_product_attention`.
  - *Дебаг:* Forward pass на фіксованому сіді має давати ідентичні логіти (точність 1e-4).
- [ ] **2.3 Mixed Precision (AMP):** Додати `torch.autocast` у цикл навчання.
  - *Дебаг:* Заміряти швидкість (iters/sec) та утилізацію VRAM (nvidia-smi).
- [ ] **2.4 JIT Компіляція:** Додати `torch.compile(model)` перед початком навчання.
  - *Дебаг:* Порівняти час першого кроку та наступних.

## Фаза 3: Оптимізація інференсу (KV-Cache)
- [ ] **3.1 Forward Pass Update:** Додати підтримку аргументу `past_key_values`.
  - *Дебаг:* Переконатися, що навчання без кешу працює стабільно.
- [ ] **3.2 Кешування станів:** Реалізувати повернення поточних `Key` та `Value` у кожному шарі.
  - *Дебаг:* Перевірити розмірності кешу `[B, n_head, T, head_size]`.
- [ ] **3.3 Ефективна генерація:** Оновити `generate()` для подачі лише останнього токена.
  - *Дебаг:* Заміряти час генерації 1-го та 200-го токена (мають бути майже однакові).

## Фаза 4: Операційна стійкість
- [ ] **4.1 CLI Interface:** Впровадити `argparse` для керування гіперпараметрами.
  - *Дебаг:* Запустити через `--batch_size 128` і перевірити зміни.
- [ ] **4.2 Checkpointing:** Додати автоматичне збереження `model.state_dict()` та стану оптимізатора.
  - *Дебаг:* Перевірити наявність файлу `.pt` після завершення епохи.
- [ ] **4.3 Resume Training:** Реалізувати логіку завантаження з останнього чекпоінту.
  - *Дебаг:* Перервати навчання і продовжити — Loss curve має бути плавною.

